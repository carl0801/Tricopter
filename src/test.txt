#include <Arduino.h>
#include "IMU.h"

IMU imu;

float eu[3];

void setup() {
    Serial.begin(1000000);
    imu.init();

}

void loop() {
 
    imu.update_IMU();
    imu.getEulerRad(&eu[0], &eu[1], &eu[2]);
    imu.sendToPC(&eu[0], &eu[1], &eu[2]);

    /*Serial.print(eu[0]);
    Serial.print(" ");
    Serial.print(eu[1]);
    Serial.print(" ");
    Serial.println(eu[2]);*/

}